name: latinize
version: 0.0.1
description:
  Simple library to convert accents (diacritics) from strings to latin characters. 
homepage: https://github.com/latinize/latinize

environment:
  sdk: '>=2.0.0 <3.0.0'

dev_dependencies:
  test: ^1.5.1+1
